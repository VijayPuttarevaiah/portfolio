# Two logos still missing

`amazon.svg`, `dalhousie.svg` and `wipro.svg` are already here (public domain,
Wikimedia Commons).

Still needed, if you have them:

| Filename       | Organisation        | Currently shows as |
|----------------|---------------------|--------------------|
| `acuver.svg`   | Acuver Consulting   | "AC" monogram      |
| `nie.svg`      | NIE                 | "NIE" monogram     |

`.png` works as well. Once a file lands here it gets wired into
`src/components/BrandMark.tsx` and replaces the monogram automatically.
